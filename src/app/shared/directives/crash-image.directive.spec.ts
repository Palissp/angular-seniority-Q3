import { CrashImageDirective } from './crash-image.directive';

describe('CrashImageDirective', () => {
  it('should create an instance', () => {
    const directive = new CrashImageDirective();
    expect(directive).toBeTruthy();
  });
});
